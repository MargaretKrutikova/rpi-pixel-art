{
  "name": "rpi-pixel-art",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "clean": "lerna run clean",
    "build": "lerna run build",
    "pub": "lerna publish",
    "deploy:client": "npm run bootstrap && npm --prefix ./packages/client run webpack:production "
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "typescript": "^3.9.5"
  }
}
